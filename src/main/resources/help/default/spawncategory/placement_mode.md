Selects how this category provides default spawn-placement rules to Living Entities.

- **PRESET** uses one of the built-in rule combinations.
- **CUSTOM** lets you choose the placement location, heightmap, and default spawn condition independently.

A Living Entity's custom spawning-condition procedure still overrides the category's default condition.
