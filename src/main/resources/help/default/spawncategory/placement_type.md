Controls where Minecraft considers a candidate spawn position valid.

- **ON_GROUND** requires valid supporting terrain and empty space.
- **IN_WATER** requires water at the spawn position.
- **NO_RESTRICTIONS** skips the placement-location check.

This is separate from the default spawn condition and is used only in **CUSTOM** mode.
